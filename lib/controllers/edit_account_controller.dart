import 'package:flutter/material.dart';

class EditAccountController extends StatelessWidget {
  const EditAccountController({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}